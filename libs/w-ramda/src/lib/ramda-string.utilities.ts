import {
  match,
  replace,
  split,
  test,
  toLower,
  toString,
  toUpper,
  trim,
} from 'ramda'

export const rMatch = match
export const rReplace = replace
export const rSplit = split
export const rTest = test
export const rToLower = toLower
export const rToString = toString
export const rToUpper = toUpper
export const rTrim = trim
