import { chain, chainFirst, map, of, task } from 'fp-ts/Task'

export const tChain = chain
export const tChainFirst = chainFirst
export const tMap = map
export const tOf = of
export const tTask = task
