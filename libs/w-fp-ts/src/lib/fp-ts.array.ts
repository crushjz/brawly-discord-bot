import {
  difference,
  filterMap,
  intersection,
  isNonEmpty,
  of,
} from 'fp-ts/Array'

export const fpDifference = difference
export const fpFilterMap = filterMap
export const fpIntersection = intersection
export const fpIsNonEmpty = isNonEmpty
export const fpOf = of
